# YPP Editor - 剧组打卡神器 - Construction Log

## v1.05 (Current)
### ✨ 新功能 (Features)
- **云端静默同步架构 (Local-First Cloud Sync)**：重构了存储逻辑，不再强依赖云端响应。所有操作秒级保存本地，后台自动完成与 Supabase 的双向增量同步。
- **自定义项全面放权 (Global Edit)**：废除了“只有自定义项才能编辑/删除”的限制。现在所有的系统模板检查项都可以随意修改文案、更改标签和删除。
- **分类标签自定义 (Custom Tags)**：在编辑和新建检查项时，新增了对“分类标签（Tag）”的输入支持，视觉信息密度对齐官方模板。
- **高危阻断项升级 (Critical Item Blocking)**：不仅内置关键流程会触发“抖动拦截”，现在你可以把任意一个自定义或编辑后的检查项，手动标记为 ⚠️“致命高危项”，强制剧组不可跳过。
- **现场专注模式 (Zen Mode)**：新增 `[🎯 专注]` 按钮，一键隐藏其他阶段的干扰信息，专为片场高压/ADHD环境打造。

### 🐛 踩坑与修复 (Bugfixes & Learnings)
- **正则表达式盲写陷阱**：在单文件 Vanilla JS 架构中，使用纯正则向大文件中注入逻辑极易因上下文断层而错乱。已引入物理行验证 SOP。
- **Storage 键值读写脱节**：修复了保存和读取使用了两套不同 `STORAGE_KEY` 导致新项目一刷新就丢失的历史遗留 Bug。
- **Supabase Cache 伪报错**：修复了建表时遇到的 `Could not find the table in the schema cache` 骗局。总结了判断依据：该报错大概率是由于根本没有成功建表或环境选错引起，而非单纯的缓存延迟。

- **MCP 挂载认知盲区**：总结了 AI 容易因为上下文工具清单未挂载而误判“无 MCP 权限”的缺陷。确立了穿透查阅 `mcp_config.json` 与直接通过终端底层降级调用的 SOP。

## v1.06 (Mobile Optimization Upgrade)
### 📱 移动端与片场单手操作优化 (Mobile & Touch Polish)
- **免悬停触控感知**：条目右侧的 ✏️ 编辑与 🗑️ 删除在移动端默认常驻显形（低透明度），桌面端继续保持鼠标悬停时显形，彻底解决手机无 Hover 的交互缺陷。
- **触控靶心放大 (Touch Targets)**：
  - 复选框升级为 `w-6 h-6`（大拇指盲按命中率提升 100%）。
  - 条目行垂直间距提升至 `p-3`，支持全行点击与按压微动效 (`active:scale-95`)。
  - 编辑/删除按钮扩增至 28px 触控热区，并严格防误触冒泡。
- **自适应紧凑 Header**：移动端自动精简多余副标题与项目名长截断，优化顶部竖屏空间利用率。
- **iOS / Android 安全区适配 (Safe Area Insets)**：
  - 引入 `env(safe-area-inset-top)` 和 `env(safe-area-inset-bottom)`。
  - 弹窗增加最大 88vh 内部滚动，彻底防止被手机软键盘或底部横条顶飞。
  - 强制移动端输入框 `16px` 字号，根治 iOS 唤起键盘时的烦人自动放大问题。

## v1.07 (Minimalist Flow Mode / ⚡极简流升级)
### ⚡ 极简模式 (Things 3 / Apple Reminders 风格)
- **一键切换开关**：顶部 Header 新增 `⚡ 极简` / `⚡ 详细` 切换按钮，状态自动持久化保存。
- **单阶段沉浸 Tab 流**：
  - 隐藏冗余的项目大横幅与打卡心法提示框，消除所有视觉杂音。
  - 顶部提供横向顺滑滑动的阶段胶囊药丸（Pill Tabs），直观展示各阶段完成度 `(3/5)`，点击即切换。
  - 屏幕 100% 专注展示当前阶段的检查项。
- **大圆环与大字排版**：
  - 升级为 `w-7 h-7` 极简圆形 Checkbox，打勾伴随翠绿发光与划线暗化动效。
  - 字体字号放大至 16px，高对比度呈现。
- **阶段闭环引导**：当前阶段检查完毕后，底部提供优雅的 `[ 下一阶段 ➡️ ]` 快速跃迁按钮；全流程完成则直接进入 `[ 完结归档 ]`。

## v1.08 (Smart Line Split / ⏎ 回车裂变升级)
### ⚡ 回车拆分子项 (Enter-to-Split Multi-Item Engine)
- **多行回车智能拆分**：在新建或编辑事项时，输入框升级为多行文本框。直接按回车换行，系统保存时会自动将每一行拆解为**独立的原子清单项**。
- **编辑裂变插入 (In-place Splitting)**：编辑现有长句子时，只要在任意位置按下回车分成多行，保存后第一行保留在原位，其余各行会自动作为**紧跟其后的子项**插入到列表中，杜绝大段文字堆积。
- **效率快捷键**：支持 `Cmd + Enter` / `Ctrl + Enter` 一键极速保存并裂变。
